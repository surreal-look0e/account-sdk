# Copy to .env and adjust as needed:
# cp .env.example .env

# Optional RPC for local/dev
RPC_URL=http://localhost:8545

# Optional app name used in wallet connection UI (if applicable)
APP_NAME=Account SDK Demo

# If your repo uses a CDP / API key, set it here (optional)
# CDP_API_KEY=your_key_here
